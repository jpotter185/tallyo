Deployed on Vercel at this link: https://tallyo.us

## API Types

Generate frontend API types from backend OpenAPI spec:

```bash
npm run gen:api-types
```

This reads `../tallyo-backend/openapi.yaml` and writes `src/types/api.generated.ts`.
